"use client";import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {getSession,setSession} from "@/lib/session";
const map:any={ADMIN:['/','/data','/quality','/anomalies','/agent','/reports','/audit','/users'],ANALISTA:['/','/data','/quality','/anomalies','/agent','/reports'],GESTOR:['/','/data','/quality','/anomalies','/agent','/reports','/audit'],AUDITOR:['/','/data','/quality','/anomalies','/agent','/reports','/audit']};
const labels:any={'/':'Visão geral','/data':'Dados','/quality':'Qualidade','/anomalies':'Anomalias','/agent':'Agente','/reports':'Relatórios','/audit':'Auditoria','/users':'Usuários'};
export default function Shell({children}:{children:React.ReactNode}){const s=getSession(),path=usePathname(),router=useRouter();if(!s)return <>{children}</>;return <div className="shell"><aside className="sidebar"><div className="brand"><span>I</span><div><b>InsightOps AI</b><small>Operational intelligence</small></div></div><nav>{map[s.user.role].map((x:string)=><Link className={path===x?'active':''} href={x} key={x}>{labels[x]}</Link>)}</nav><div className="session"><b>{s.user.name}</b><small>{s.user.role}</small></div><button className="linkButton" onClick={()=>{setSession(null);router.push('/login')}}>Sair</button></aside><main><header className="topbar"><span className="pill">dataset: demo-2026-07</span><span className="pill">safe tools only</span></header><section className="content">{children}</section></main></div>}
