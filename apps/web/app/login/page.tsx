"use client";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {api} from "@/lib/api";import {setSession} from "@/lib/session";
const labels:any={ADMIN:'Administrador de Plataforma',ANALISTA:'Analista de Dados',GESTOR:'Gestora de Operações',AUDITOR:'Auditor de Conformidade'};
export default function Login(){const [users,setUsers]=useState<any[]>([]),router=useRouter();useEffect(()=>{api('/auth/users').then(setUsers)},[]);async function go(id:string){const s:any=await api('/auth/login',{method:'POST',body:JSON.stringify({user_id:id})});setSession(s);router.push('/')}return <div className="login"><div className="loginCard"><section className="loginHero"><div className="eyebrow">InsightOps AI</div><h1>Inteligência operacional com evidências.</h1><p className="muted">Dados sintéticos, métricas determinísticas e agente restrito a tools seguras.</p><div className="alert">100% local. Nenhum serviço pago é obrigatório.</div></section><section className="loginPanel"><h2>Entrar como usuário de demonstração</h2><div className="roleGrid">{users.map(u=><button className="roleBtn" key={u.id} onClick={()=>go(u.id)}><b>{u.name}</b><span>{labels[u.role]}</span></button>)}</div></section></div></div>}
