"use client";import {useEffect,useState} from "react";import PageHead from "@/components/PageHead";import {api} from "@/lib/api";export default function Quality(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/quality/rejections').then(setRows)},[]);return <><PageHead eyebrow="Data quality" title="Qualidade" description="Registros inválidos são isolados com regra, campo, valor e lineage; nenhuma correção silenciosa."/><div className="card"><table><thead><tr><th>Linha</th><th>Carga</th><th>Campo</th><th>Valor</th><th>Regra</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>#{r.row}</td><td>{r.load_id}</td><td>{r.field}</td><td>{r.value||'(vazio)'}</td><td>{r.rule}</td></tr>)}</tbody></table>{!rows.length&&<div className="alert">Nenhuma rejeição persistida ainda. Importe um CSV com linhas inválidas para demonstrar a quarentena.</div>}</div></>}
